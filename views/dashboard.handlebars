<h1>Your Dashboard</h1>
{{#each posts}}
    <div>
        <h2>{{this.title}}</h2>
        <p>{{this.content}}</p>
        <button>Edit</button>
        <button>Delete</button>
    </div>
{{/each}}
<button>Create New Post</button>
